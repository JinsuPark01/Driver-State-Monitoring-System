<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:background="@drawable/bg_drive_card"
    android:layout_marginBottom="6dp">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- 배차 날짜 -->
        <TextView
            android:id="@+id/tvDispatchDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="25/9/30 09:00"
            android:fontFamily="@font/pretendard_bold"
            android:textSize="16sp"
            android:layout_marginEnd="1dp"
            android:textStyle="bold"/>

        <!-- 알림 메시지 -->
        <TextView
            android:id="@+id/tvNotificationMessage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="에 신규 배차 추가"
            android:fontFamily="@font/pretendard_regular"
            android:textSize="14sp"
            />
        <TextView
            android:id="@+id/tvNotificationIsRead"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="읽음"
            android:fontFamily="@font/pretendard_semibold"
            android:textSize="14sp"
            android:textColor="#333"/>
    </LinearLayout>

    <TextView
        android:id="@+id/tvNotificationCreatedAt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="2025-09-28 23:41"
        android:fontFamily="@font/pretendard_regular"
        android:textSize="12sp"
        android:textColor="@color/gray_light"
        android:layout_marginTop="4dp"
        />
</LinearLayout>
